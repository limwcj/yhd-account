{
  "watch": ["src"],
  "ext": "ts,graphql,proto",
  "ignore": ["src/**/*.spec.ts", "src/schema.graphql"],
  "env": {
    "NODE_ENV": "development"
  },
  "exec": "ts-node -r dotenv/config --files -r tsconfig-paths/register src/main.ts"
}
